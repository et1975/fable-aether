{
    "private": true,
    "name": "aether-tests",
    "version": "0.0.0",
    "description": "Tests for fable-aether",
    "main": "index.js",
    "scripts": {
        "build": "fable",
        "test": "fable && mocha \"build/tests/*.js\"",
        "test-debug": "mocha \"build/tests/*.js\" --debug-brk"
    },
    "devDependencies": {
        "fable-aether": "^0.1.1",
        "fable-compiler": "^0.7.16",
        "fable-core": "^0.7.11",
        "fable-plugins-nunit": "^0.7.1",
        "mocha": "^3.0.2"
    }
}
